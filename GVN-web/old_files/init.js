//map initialization

var map = L.map('map').setView([39.080065, -108.554671], 16);
//map.locate({setView: true, maxZoom: 16});    //Enable this line in production to allow a popup
                                               //that asks for the users location